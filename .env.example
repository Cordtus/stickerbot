BOT_TOKEN=

# Public Telegram is the development default. Production can use the shared local service.
TELEGRAM_API_ROOT=https://api.telegram.org
# Required only when the local Bot API returns absolute file_path values.
TELEGRAM_FILE_ROOT=
